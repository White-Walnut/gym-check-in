{
  "name": "gym-check-in",
  "version": "1.9.8",
  "description": "Offline RFID gym check-in kiosk",
  "main": "src/main.js",
  "author": "",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "electron .",
    "dev": "electron . --dev",
    "test": "node --test",
    "dist": "electron-builder --win",
    "version": "node scripts/update-changelog.js",
    "publish-release": "node scripts/publish-release.js",
    "release": "npm version patch && git push && git push --tags && npm run dist && npm run publish-release",
    "release:minor": "npm version minor && git push && git push --tags && npm run dist && npm run publish-release",
    "release:major": "npm version major && git push && git push --tags && npm run dist && npm run publish-release"
  },
  "dependencies": {
    "electron-updater": "6.8.9"
  },
  "devDependencies": {
    "electron": "44.0.0",
    "electron-builder": "26.15.3"
  },
  "build": {
    "appId": "local.gym.checkin",
    "productName": "Gym Check-in",
    "artifactName": "Gym-Check-in-Setup-${version}.${ext}",
    "asar": true,
    "files": [
      "src/**/*",
      "assets/**/*",
      "package.json"
    ],
    "win": {
      "icon": "assets/icon.png",
      "target": "nsis"
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "uninstallDisplayName": "Gym Check-in"
    },
    "publish": {
      "provider": "github",
      "owner": "White-Walnut",
      "repo": "gym-check-in",
      "releaseType": "release"
    }
  }
}
